<div class="w-50 mx-auto">
    {{#each equipos}}
    <a href="#" id="{{this.idequipo}}" class="aEquipo">
        <li class="list-group-item d-flex justify-content-between align-items-center">
            {{this.nombre}}
            <span class="badge badge-primary badge-pill">{{this.nomdeporte}}</span>
        </li>
    </a>
    {{/each}}
    </ul>
</div>
</div>